# T-008-03 Progress: Remove CLI Placeholder Commands

## Completed

1. Updated module doc comment — removed "moron preview" reference
2. Removed `Preview`, `Init`, `Gallery` variants from `Commands` enum
3. Removed three placeholder match arms from `main()`
4. Verified: `cargo check` passes, `cargo test` passes (175 tests), `cargo clippy` clean

## Deviations

None. Plan executed as written.
